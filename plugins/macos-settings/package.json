{
	"name": "@sidequest/macos-settings",
	"version": "1.0.0",
	"private": true,
	"description": "macOS settings expert for Claude Code",
	"scripts": {
		"test": "echo 'No tests required for markdown-only plugin'",
		"typecheck": "echo 'No TypeScript in this plugin'",
		"format": "echo 'No formatting required'",
		"lint": "echo 'No linting required'",
		"check": "echo 'Markdown-only plugin - validation via claude plugin validate'"
	}
}
