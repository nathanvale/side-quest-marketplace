{
  "name": "@sidequest/para-brain",
  "version": "1.0.0",
  "private": true,
  "description": "PARA method and Building a Second Brain plugin for Obsidian knowledge management",
  "type": "module",
  "scripts": {
    "test": "echo 'No tests yet'",
    "typecheck": "echo 'No TypeScript files'",
    "format": "echo 'Markdown files only'",
    "format:check": "echo 'Markdown files only'",
    "lint": "echo 'Markdown files only'",
    "check": "echo 'Markdown files only'"
  }
}
