/**
 * Formatters module - Output formatting utilities
 *
 * Re-exports from output.ts for consistent module structure.
 *
 * @example
 * ```ts
 * import { formatJson, formatTable, formatMarkdownTable } from "@sidequest/core/formatters";
 * ```
 */

export * from "./output.js";
